# Disambiguation-note checklist

> Generated by `npm run data:convert` — do not hand-edit. Auto-linked groups
> (exact-match English) where at least one member still lacks a note in
> `disambiguation-notes.json`. SPEC §4: each flagged group needs a human-written,
> owner-approved note before it silently ships empty.

Total auto-linked groups: 28 · still needing notes: 28

| English | Words | Missing notes |
|---|---|---|
| excuse me | perdón (`greetings_perdón`), disculpe (`greetings_disculpe`), con permiso (`greetings_con-permiso`) | 3/3 |
| of course | claro (`greetings_claro`), por supuesto (`greetings_por-supuesto`) | 2/2 |
| you | tú (`greetings_tú`), usted (`greetings_usted`) | 2/2 |
| this | este (`greetings_este`), esta (`greetings_esta`), esto (`greetings_esto`) | 3/3 |
| that | ese (`greetings_ese`), esa (`greetings_esa`), eso (`greetings_eso`) | 3/3 |
| these | estos (`greetings_estos`), estas (`greetings_estas`) | 2/2 |
| those | esos (`greetings_esos`), esas (`greetings_esas`) | 2/2 |
| soft | suave (`colors_suave`), blando (`physical_blando`) | 2/2 |
| fish | pez (`animals_pez`), pescado (`food_pescado`) | 2/2 |
| cricket | grillo (`animals_grillo`), críquet (`sports_críquet`) | 2/2 |
| rib | costilla (`food_costilla`), costilla (`body_costilla`) | 2/2 |
| fan | ventilador (`house_ventilador`), aficionado (`sports_aficionado`) | 2/2 |
| cousin | prima (`family_prima`), primo (`family_primo`) | 2/2 |
| doctor | doctor (`health_doctor`), médico (`professions_médico`) | 2/2 |
| nurse | enfermera (`health_enfermera`), enfermero (`professions_enfermero`) | 2/2 |
| student | estudiante (`professions_estudiante`), alumno (`school_alumno`) | 2/2 |
| company | empresa (`work_empresa`), compañía (`shopping_compañía`) | 2/2 |
| ticket | entrada (`hobbies_entrada`), billete (`travel_billete`) | 2/2 |
| school | escuela (`school_escuela`), colegio (`school_colegio`) | 2/2 |
| homework | tarea (`school_tarea`), deberes (`school_deberes`) | 2/2 |
| diploma | diploma (`school_diploma`), bachillerato (`school_bachillerato`) | 2/2 |
| to pass | aprobar (`school_aprobar`), pasar (`core_pasar`) | 2/2 |
| computer | computadora (`technology_computadora`), ordenador (`technology_ordenador`) | 2/2 |
| first | primero (`numbers_primero`), primero (`question_primero`) | 2/2 |
| to walk | andar (`core_andar`), caminar (`actions_caminar`) | 2/2 |
| to know | conocer (`core_conocer`), saber (`core_saber`) | 2/2 |
| to be | ser (`core_ser`), estar (`core_estar`) | 2/2 |
| when | cuándo (`question_cuándo`), cuando (`question_cuando`) | 2/2 |
